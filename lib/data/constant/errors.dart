
import 'package:flutter_http_cat/data/model/error.dart';

const internalError = MyError("Internal Error!");

const serverError = MyError("Server Error!");

const networkError = MyError("Network Error!");